# User context template

This is a tracked template only. The populated user context belongs in ignored
runtime state unless the user explicitly chooses to publish a non-sensitive
preference.

Never store secrets, credentials, mailbox tokens, personal document contents,
or mutable security policy here.
