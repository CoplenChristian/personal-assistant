# Prompt router hook

Deterministically match dashboard, message, scheduler, and agent-to-agent
inputs to canonical skills. Prefer keyword/rule matching; do not add another
LLM solely to choose a skill.
