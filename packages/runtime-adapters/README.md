# Runtime adapters

Adapters for the native `claude` and `codex` commands. The contract should
cover start, stop, restart, prompt delivery, pane capture, status, compact,
clear, resume, rotation, and session-size measurement.

Adapters must not emulate a vendor protocol or call a model API.
