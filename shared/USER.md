# User context

Keep durable user preferences here only when the user has explicitly chosen to
store them. Secrets, credentials, mailbox tokens, and sensitive mutable policy
state do not belong in this repository.
