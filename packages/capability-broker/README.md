# Capability broker

The narrow local RPC boundary for consequential integrations. Every request
must include an agent ID, realm, capability, operation, parameters, and
request ID. The broker enforces policy independently of skill instructions.

Initial capability domains are mail, calendar, reminders, messaging,
documents, memory, agents, and browser.
